"use client";

export default function ProfileXPHistory({ user }) { const goal = user.dailyGoal || 10; const today = user.dailyXp || 0; const percent = Math.min(100, Math.round((today / goal) * 100)); return <section className="cosmic-bright-card" style={{ padding: 18, borderRadius: 20 }}><h2 style={{ marginTop: 0 }}>XP rhythm</h2><p style={{ margin: 0, color: "#c3d3e9", fontSize: 13 }}>Today: {today}/{goal} XP · Total: {user.xp || 0} XP</p><div style={{ height: 10, overflow: "hidden", margin: "12px 0", borderRadius: 99, background: "rgba(255,255,255,.1)" }}><div style={{ width: `${percent}%`, height: "100%", borderRadius: "inherit", background: "linear-gradient(90deg,#ffda78,#a47cff)" }} /></div><small style={{ color: percent >= 100 ? "#aaf3cb" : "#aebfda" }}>{percent >= 100 ? "Daily goal complete!" : `${100 - percent}% to today’s goal`}</small></section>; }
