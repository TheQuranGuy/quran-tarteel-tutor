"use client";

export default function ProfileStreakHistory({ streak, lastActive }) { const days = ["M", "T", "W", "T", "F", "S", "S"]; const activeCount = Math.min(7, streak || 0); return <section className="cosmic-bright-card" style={{ padding: 18, borderRadius: 20 }}><h2 style={{ marginTop: 0 }}>Streak rhythm</h2><div style={{ display: "flex", justifyContent: "space-between", gap: 7 }}>{days.map((day, index) => <div key={`${day}-${index}`} style={{ display: "grid", placeItems: "center", gap: 5 }}><span style={{ width: 29, height: 29, display: "grid", placeItems: "center", borderRadius: "50%", color: index >= 7 - activeCount ? "#ffde8b" : "#7890b6", background: index >= 7 - activeCount ? "rgba(255,181,69,.2)" : "rgba(255,255,255,.06)" }}>{index >= 7 - activeCount ? "🔥" : "·"}</span><small style={{ color: "#aebfda" }}>{day}</small></div>)}</div><p style={{ margin: "13px 0 0", color: "#c3d3e9", fontSize: 13 }}>Current streak: {streak || 0} days{lastActive ? ` · Last active ${lastActive}` : ""}</p></section>; }
