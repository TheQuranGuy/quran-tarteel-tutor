"use client";

import Link from "next/link";
import { getCosmicProgress } from "../../../../lib/cosmic";

export default function GalaxySurahInfo({ surah, user, onAyahSelect }) { if (!surah) return null; const progress = getCosmicProgress(user, surah); return <aside style={{ pointerEvents: "auto", width: 270, padding: 16, border: "1px solid rgba(184,219,255,.2)", borderRadius: 18, color: "#eaf5ff", background: "rgba(8,15,44,.8)", backdropFilter: "blur(12px)" }}><small style={{ color: "#a8c7ff", fontWeight: 900 }}>SURAH PLANET</small><h2 style={{ margin: "6px 0" }}>{surah.name}</h2><p style={{ margin: "0 0 12px", color: "#c4d1e8", fontSize: 13 }}>{surah.theme}</p><div style={{ height: 7, overflow: "hidden", borderRadius: 99, background: "rgba(255,255,255,.12)" }}><div style={{ width: `${progress.percent}%`, height: "100%", background: "#9ff2ca" }} /></div><small style={{ display: "block", margin: "7px 0 13px" }}>{progress.percent}% mastery</small><div style={{ display: "flex", gap: 8, flexWrap: "wrap" }}><Link href={`/surah/${surah.id}`} style={{ padding: "9px 10px", borderRadius: 11, color: "#0b1e2a", background: "#9ff2ca", textDecoration: "none", fontSize: 13, fontWeight: 900 }}>Open Surah</Link><button type="button" onClick={() => onAyahSelect(surah.ayahs[0])} style={{ border: "1px solid rgba(184,219,255,.28)", borderRadius: 11, padding: "9px 10px", cursor: "pointer", color: "#eaf5ff", background: "transparent", fontSize: 13, fontWeight: 900 }}>First ayah</button></div></aside>; }
